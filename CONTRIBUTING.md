# دليل المساهمة في مشروع نظام نقاط البيع للمطاعم

نرحب بمساهماتكم القيمة في تطوير وتحسين نظام نقاط البيع للمطاعم. يهدف هذا الدليل إلى توضيح كيفية المساهمة بفعالية في المشروع، سواء كان ذلك بالإبلاغ عن الأخطاء، اقتراح الميزات، أو كتابة الكود.

## الإبلاغ عن الأخطاء (Bug Reports)

إذا واجهت أي خطأ أو مشكلة في النظام، يرجى اتباع الخطوات التالية للإبلاغ عنها:

1.  **البحث عن الأخطاء الموجودة**: قبل فتح مشكلة جديدة، يرجى التحقق من قائمة [المشاكل (Issues)](https://github.com/SMSMy/POS/issues) للتأكد من أن الخطأ لم يتم الإبلاغ عنه مسبقًا.
2.  **فتح مشكلة جديدة**: إذا لم تجد الخطأ، يرجى فتح [مشكلة جديدة](https://github.com/SMSMy/POS/issues/new/choose) باستخدام قالب الإبلاغ عن الأخطاء المتاح.
3.  **تضمين التفاصيل**: يرجى تقديم أكبر قدر ممكن من التفاصيل، بما في ذلك:
    *   خطوات إعادة إنتاج الخطأ.
    *   السلوك المتوقع والسلوك الفعلي.
    *   إصدار النظام الذي تستخدمه.
    *   لقطات شاشة أو تسجيلات فيديو (إن أمكن).

## اقتراح الميزات (Feature Suggestions)

نرحب بأفكاركم ومقترحاتكم لتحسين النظام وإضافة ميزات جديدة. لتقديم اقتراح، يرجى:

1.  **البحث عن الميزات المقترحة**: تحقق من [المشاكل (Issues)](https://github.com/SMSMy/POS/issues) للتأكد من أن الميزة لم يتم اقتراحها مسبقًا.
2.  **فتح مشكلة جديدة**: افتح [مشكلة جديدة](https://github.com/SMSMy/POS/issues/new/choose) باستخدام قالب اقتراح الميزات.
3.  **وصف الميزة**: اشرح الميزة المقترحة بوضوح، مع توضيح سبب أهميتها وكيف يمكن أن تفيد المستخدمين.

## المساهمة بالكود (Code Contributions)

إذا كنت ترغب في المساهمة بالكود، يرجى اتباع سير العمل التالي:

1.  **تفرع المستودع (Fork the Repository)**: قم بعمل `fork` للمستودع إلى حسابك على GitHub.
2.  **استنساخ المستودع (Clone the Repository)**: استنسخ المستودع المتفرع إلى جهازك المحلي:
    ```bash
    git clone https://github.com/YOUR_USERNAME/POS.git
    cd POS
    ```
3.  **إنشاء فرع جديد (Create a New Branch)**: أنشئ فرعًا جديدًا لعملك:
    ```bash
    git checkout -b feature/your-feature-name
    # أو
    git checkout -b bugfix/your-bugfix-name
    ```
4.  **إجراء التغييرات**: قم بإجراء التغييرات المطلوبة في الكود.
5.  **اختبار التغييرات**: تأكد من أن تغييراتك تعمل بشكل صحيح ولا تسبب مشاكل جديدة. يفضل كتابة اختبارات جديدة للتغييرات التي أجريتها.
6.  **تثبيت التغييرات (Commit Changes)**: قم بتثبيت تغييراتك برسالة واضحة وموجزة:
    ```bash
    git commit -m "feat: إضافة ميزة جديدة" # للميزات الجديدة
    # أو
    git commit -m "fix: إصلاح خطأ في كذا" # لإصلاح الأخطاء
    ```
7.  **رفع التغييرات (Push Changes)**: ارفع الفرع الجديد إلى مستودعك المتفرع:
    ```bash
    git push origin feature/your-feature-name
    ```
8.  **فتح طلب سحب (Open a Pull Request)**: افتح [طلب سحب (Pull Request)](https://github.com/SMSMy/POS/pulls) من فرعك إلى الفرع الرئيسي (main) للمستودع الأصلي. يرجى وصف التغييرات التي أجريتها بوضوح في وصف طلب السحب.

## معايير الكود (Code Standards)

*   **التنسيق**: يرجى الالتزام بمعايير تنسيق الكود الموجودة في المشروع. يمكنك استخدام `black` لتنسيق الكود تلقائيًا:
    ```bash
    black src/
    ```
*   **فحص الأخطاء**: يرجى التأكد من عدم وجود أخطاء في الكود باستخدام `flake8`:
    ```bash
    flake8 src/
    ```
*   **التعليقات**: يفضل إضافة تعليقات توضيحية للكود المعقد أو غير الواضح.

نشكركم على اهتمامكم ومساهمتكم في جعل هذا المشروع أفضل!
